<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>

    
 <rewrite>
  <rules>

    <rule name="ExcludeLegacyApps" stopProcessing="true">
      <match url="^(DUBAI|DUBAICC|RealEstate|services|blogs|APIS)(/.*)?$" />
      <action type="None" />
    </rule>

    <rule name="NextStaticFiles" stopProcessing="true">
      <match url="^_next/(.*)$" />
      <action type="Rewrite" url="http://localhost:5000/_next/{R:1}" />
    </rule>

    <rule name="NextIcons" stopProcessing="true">
      <match url="^icons/(.*)$" />
      <action type="Rewrite" url="http://localhost:5000/icons/{R:1}" />
    </rule>

    <rule name="PublicFolderFiles" stopProcessing="true">
      <match url="^public/(.*)$" />
      <action type="Rewrite" url="http://localhost:5000/public/{R:1}" />
    </rule>

    <!-- ⭐ THIS FIXES YOUR API ERROR ⭐ -->
    <rule name="NextApi" stopProcessing="true">
      <match url="^api/(.*)$" />
      <action type="Rewrite" url="http://localhost:5000/api/{R:1}" />
    </rule>

    <!-- Main rule -->
    <rule name="NextJSRewrite" stopProcessing="true">
      <match url="(.*)" />
      <action type="Rewrite" url="http://localhost:5000/{R:1}" />
    </rule>

  </rules>
</rewrite>


  </system.webServer>
</configuration>
